@model IEnumerable<KernelTravelGuides.Models.Province>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.province_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.province_image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.province_status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.created_at)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.updated_at)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.province_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.province_image)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.province_status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.created_at)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.updated_at)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.province_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.province_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.province_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
